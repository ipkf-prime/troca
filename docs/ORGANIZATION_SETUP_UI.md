# Organization Setup UI

Operational workspace at `/admin/organization-setup` for creating organization hierarchy, organization units, organization positions, and linking a User account to a Person. All public UI references use UUID public references; raw database IDs remain internal.
