# Organizational Operations UI

Operational admin pages:

- `/admin/organization-chart` — organization/unit/position tree and current occupants.
- `/admin/appointments` — create and list dated appointments without hard deletion.
- `/admin/profile/organizational-context` — select the authenticated user's active appointment.

Organizational context remains separate from RBAC and does not grant permissions.
